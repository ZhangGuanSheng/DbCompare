<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper  namespace="mybatis.entity.SysUserInfo">
    <!-- 查询全部用户 -->
    <select id="queryUsers" resultType="mybatis.entity.SysUserInfo">
  		select * from SysUserInfo
  	</select>
    <!-- 按照ID查询 -->
    <!-- <select id="queryUsersId" parameterType="int" resultType="com.entity.Users">
  		select * from users  where id=#{id}
  	</select> -->

    <!-- 删除用户 -->
    <!-- <delete id="deleteUsers" parameterType="com.entity.Users">
  		delete  users  where id=#{id}
  	</delete> -->

</mapper>